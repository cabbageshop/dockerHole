# dockerHole

A Docker container designed to function as a Tailscale exit node with integrated ad-blocking capabilities, similar to Pi-hole. Devices connecting to Tailscale using this container as an exit node will benefit from network-wide ad blocking.
